# Studio Lens — REVIEW template. The find→refute→synth panel, fully model-driven: the SELECT step AUTHORS the
# review lenses from the code itself (zero enum, the whole point of the refactor), the refute stage is the
# backstop against a fabricated lens. Orchestration is data; the engine owns the domain projections (§3).
name: review
inputs: [paths, diff, content, baseRef, task, implementerSummary, writtenFiles, floorVerdict]
enabledSetting: gateB.studioLens.enabled
# breadthInput is the caller's ENTRY INTENT, NOT a breadth level: 'thorough' = an EXPLICIT review request
# (skip the throttle, always panel); 'conservative' = the Gate-B floor-amplifier (run the escalate throttle
# first). The actual breadth — HOW MANY lenses — is the SELECT step's OWN model-decided call: it authors the
# count from the code, never a fixed 2-value clause injected by the caller (was vars.breadthClause).
breadthInput: thorough
phases:
  # ESCALATE — the Gate-B throttle (only 'conservative' entry runs it; explicit review skips straight to select).
  # Fail-open: a missing/garbled verdict → escalate=true (never silently drop the amplifier).
  - id: escalate
    type: agent
    role: reviewer
    when: "${breadthInput == 'conservative'}"
    output:
      escalate: bool
    prompt: |
      An independent FLOOR reviewer already verified this change. Decide whether to ESCALATE to a
      multi-perspective PANEL — several reviewers each probing a distinct risk axis the floor does not
      scrutinize at depth. The panel is an AMPLIFIER for SUBSTANTIAL work; a small, contained change is
      already covered by the floor alone. {{writtenFiles.length}} file(s) touched across
      {{writtenFiles.topLevelAreas.length}} top-level area(s); floor verdict: {{floorVerdict}}.
      Judge from the workload and the floor's own read — there is NO fixed file-count threshold. A broad,
      cross-cutting, or risk-laden change → ESCALATE: YES; a small single-area edit the floor already covers
      → ESCALATE: NO. When genuinely unsure, prefer YES (the floor is the safety net either way).
      End your reply with exactly one final line: `ESCALATE: YES` or `ESCALATE: NO` — nothing after it.

  # SELECT — the model AUTHORS the review lenses from the code. No fixed list, no whitelist (the refute stage
  # is the backstop against a fabricated lens). Replaces the deleted enum + deriveSubjects.
  - id: select
    type: agent
    role: reviewer
    when: "${breadthInput != 'conservative' || escalate.escalate}"
    output:
      lenses:
        - key: string
          focus: string
          why: string
    prompt: |
      You DRIVE an independent review. AUTHOR the review LENSES this CODE warrants — each a risk surface
      BEYOND standard correctness (the floor verifier already owns correctness / duplication / wrong-problem;
      do NOT re-run them). There is NO fixed list — self-derive the lenses from what THIS code actually does
      and means (a token check that can be bypassed = a security lens; a shared write without a lock = a
      concurrency lens), never from file names.

      Size the panel to THIS code's actual risk surface — author as FEW or as MANY lenses as it genuinely
      warrants: one lens (or even an empty array [] when the change is trivial and the floor already covers it)
      for a small contained edit, several for broad, cross-cutting, or risk-laden work. Don't pad the list to
      look thorough, and don't under-cover a real risk — the COUNT is yours to size to the code, not a quota.

      For each lens emit {"key":"<short-kebab-name>","focus":"<one sentence: exactly what this lens hunts
      and the failure it looks for>","why":"<one line citing the specific code — a file:hunk or concrete
      behavior — that puts it in scope>"}. The refute stage that follows drops any lens that yields no real
      defect, so over-proposing a lens is cheap and under-covering a real risk is the costly miss. NEVER
      fabricate a risk the code does not actually have.

      Task:
      {{task}}

      File(s) under review: {{paths}}

      Diff — what changed, IF available. It may be EMPTY (a brand-new file, or a project that is not a git
      repo): an empty diff is NOT "low risk" — judge entirely from the full file content below either way.
      {{diff}}

      The code as it stands — your PRIMARY source; always review THIS directly:
      {{content}}

      Return ONLY a JSON array of lens objects — no prose, no markdown fence.

  # PANEL — the find→refute PIPELINE (a real Workflow pipeline, NOT a barrier): each lens flows finder → refute
  # INDEPENDENTLY, so a fast lens's candidates are vetted while a slow lens's finder is still hunting (the old
  # two-phase `find` made EVERY finder finish before ANY refute — the 18-min coupling). All stages run under ONE
  # global concurrency limiter. The pipeline returns the folded per-lens SubjectFinding[]; candidates carry their
  # own refute verdict, so `panel.pluck(candidates).flat` is the cross-lens candidate list the result reads.
  - id: panel
    phase: Review
    type: pipeline
    over: "${select.lenses}"
    as: lens
    role: reviewer
    kit: read-only
    stages:
      # FIND stage — one finder for this lens. READS the target files directly (git diff is OPTIONAL enrichment,
      # agent's call — works on any tree, git or not), hunts its lens, emits a ```findings array with a
      # per-candidate confidence. Empty array trusted ONLY on VERDICT: PASS — a FAIL with no array degrades to
      # one prose candidate (never swallow a FAIL).
      - id: find
        card:
          name: Subject
          phase: find
          subject: "${lens.key}"
        prompt: |
          Run your "{{lens.key}}" lens on the code under review: {{lens.focus}}. Inspect it YOURSELF — READ the
          target file(s) ({{paths}}) directly; reading the code is ALWAYS enough on its own. If this happens to
          be a git repo you MAY also use `git diff {{baseRef}}` / `git status` to isolate the precise change —
          your call, only if it helps. Nothing is inlined.

          Original task:
          {{task}}

          Implementer's own summary (a claim to verify, not ground truth):
          {{implementerSummary}}
        output:
          verdict: PASS|FAIL
          candidates:
            - title: string
              file: string
              line: int
              severity: high|med|low
              confidence: high|med|low
              mechanism: string
        emit:
          contract: findings
          degradeTo: verdict
          trustEmptyOn: PASS

      # VERIFY stage — the adversarial REFUTE filter over THIS lens's OWN candidates (over: ${find.candidates},
      # the finder stage's output threaded into this stage's scope — not a cross-lens flat list). The skeptic
      # COUNT is MODEL-DECIDED: it scales to the finder's own `confidence` (votersByConfidence), so a borderline
      # candidate is vetted harder (5) than an obvious one (2). majority = floor(n/2)+1 → drop-bars high 2/2,
      # med 2/3, low 3/5: a MONOTONIC increasing drop-probability (high hardest to drop — we trust the finder —
      # low easiest), and every tier survives ONE flaky/failed vote (no even-count unanimity trap; no single vote
      # is dispositive even for high). failVote=uphold: an infra-failed / unparseable vote never drops a real defect.
      - id: verify
        phase: Verify
        type: refute
        over: "${find.candidates}"
        as: cand
        votersByConfidence: { high: 2, med: 3, low: 5 }
        failVote: uphold
        card:
          name: SubjectRefute
          phase: verify
          subject: "${cand.id}"
        system: "refutePrompt(${cand.focus})"
        prompt: |
          An independent "{{cand.lens}}" finder flagged ONE candidate defect in the code under review. As a
          SKEPTIC, try to REFUTE this single candidate — prove it is a false alarm, or concede it stands.
          Inspect it YOURSELF — READ the cited file ({{cand.file}}) directly; that is always enough. If this is a
          git repo you MAY also `git diff {{baseRef}} -- {{cand.file}}` for the precise hunk — your call.

          Candidate to refute:
          [{{cand.severity}}] {{cand.title}}
          Mechanism: {{cand.mechanism}}

          Original task:
          {{task}}

          Implementer's own summary:
          {{implementerSummary}}

  # SYNTHESIZE — the lead-reviewer turn (tool-less one-shot, the chatOnce seam): dedup the CONFIRMED findings
  # into one actionable report, severity-first. Only runs when a confirmed defect survived refute.
  - id: synth
    phase: Synthesize
    type: agent
    role: reviewer
    when: "${panel.pluck(candidates).flat.where(refuted == false).length > 0}"
    card:
      name: Synth
      phase: synth
    output:
      report: string
    prompt: |
      You are the LEAD REVIEWER. An independent panel hunted the SAME code across multiple risk lenses; every
      candidate below survived adversarial refutation (the false alarms were already dropped). Synthesize the
      CONFIRMED findings into ONE final, actionable review report for the engineer who wrote the code.

      - De-duplicate: if several lenses confirmed the SAME underlying issue, state it ONCE.
      - Lead with the real defects, most severe first. For each: WHAT is wrong, WHERE (file:line when cited),
        WHY it matters, and a concrete FIX.
      - Be tight and concrete — no preamble, no restating these instructions. This goes straight to the engineer.

      Confirmed findings (survived adversarial refutation), severity-first:
      {{panel.pluck(candidates).flat.where(refuted == false).orderBy(severity, asc)}}

# result: — the YAML归约 projection. The engine layers the domain projections (subjects[] / ok / message /
# reviewer / recordExamine payload) on top in the bridge (§3①/M-7); they are NOT expressible as归约.
result:
  confirmed: "${panel.pluck(candidates).flat.where(refuted == false).orderBy(severity, asc)}"
  refuted: "${panel.pluck(candidates).flat.where(refuted == true)}"
  candidates: "${panel.pluck(candidates).flat}"
  report: "${synth.report}"
